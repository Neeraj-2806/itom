apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-tenant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-tenant
  template:
    metadata:
      labels:
        app: redis-tenant
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-tenant
spec:
  selector:
    app: redis-tenant
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
