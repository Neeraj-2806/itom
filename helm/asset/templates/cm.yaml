apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "asset-service.fullname" . }}-configmap
  labels:
    {{- include "asset-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

