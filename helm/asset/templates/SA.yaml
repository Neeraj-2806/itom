{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "asset-service.serviceAccountName" . }}
  labels:
    {{- include "asset-service.labels" . | nindent 4 }}
{{- end }}
