{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "incident-service.serviceAccountName" . }}
  labels:
    {{- include "incident-service.labels" . | nindent 4 }}
{{- end }}
